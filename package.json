{
	"name": "collect-env",
	"version": "1.0.3",
	"description": "Collect env values and write to a file",
	"author": "JeongMin Oh",
	"license": "Apache-2.0",
	"scripts": {
		"build": "bun build --target node src/index.ts --production --outfile=dist/index.js --format=cjs",
		"lint": "biome check",
		"lint:fix": "biome check --write",
		"prepare": "husky"
	},
	"dependencies": {
		"@actions/core": "^1.11",
		"@actions/io": "^1.1"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.2",
		"@types/bun": "^1.3",
		"husky": "^9",
		"typescript": "^5.9"
	}
}
